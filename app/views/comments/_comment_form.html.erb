<% unless current_user.is_a? GuestUser %>
  <%= form_for @comment, url: '#' do |f| %>
    <div class='comment'>
      <div>
        <%= f.label :content, 'Comment', class: 'comment-label' %>
      </div>
      <div>   
        <%= f.text_area :content, class: 'comment-box' %>
      </div>
    </div>

    <%= f.submit 'Post Comment', class: 'button' %>
  <% end %>
<% end %>